@page "/one-way-bind-to-csharp"
@rendermode InteractiveServer

<PageTitle>One-way binding to C#</PageTitle>

<h1>One-way binding to C#</h1>
@* One way binding from html to C# variable*@
<p>We bind the value of the button to the currentCount variable.</p>
<code>Current count: @@currentCount</code>

<p role="status">Current count: @currentCount</p>

<button class="btn btn-primary" @onclick="IncrementCount">Click me</button>

@code {
    private int currentCount = 0;

    private void IncrementCount()
    {
        currentCount++;
    }
}
